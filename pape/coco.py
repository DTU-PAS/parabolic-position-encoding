# COCO actually only has 80 classes that are released
# but the label IDs go up to 90 with some missing IDs
# so we just set NUM_CLASSES to 91 to accommodate this
NUM_CLASSES = 91

HEIGHT = 640
WIDTH = 640
